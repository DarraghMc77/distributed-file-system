version: "3"
services:
  file_server:
    volumes:
      - '.:/Users/Darragh/College/ScalableComputing/py_dfs/distibuted-file-system/client/'
    build: .
    deploy:
      resources:
        limits:
          cpus: "0.1"
          memory: 50M
      restart_policy:
        condition: on-failure
    ports:
      - "4011:80"